{
  "name": "@idle-tamer/api",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@idle-tamer/config": "workspace:*",
    "@idle-tamer/contracts": "workspace:*",
    "@idle-tamer/database": "workspace:*",
    "fastify": "^5.10.0",
    "pino": "^9.7.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
